<Window x:Class="Gutenkern.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Gutenkern"
        Icon="AppIcon.ico"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        UseLayoutRounding="True">
    <StackPanel Margin="24">
        <Image Width="64"
               Height="64"
               HorizontalAlignment="Center"
               Source="AppIcon.ico"
               Stretch="Uniform"/>
        <TextBlock Text="Gutenkern"
                   FontSize="20"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Margin="0,8,0,0"/>
        <TextBlock x:Name="VersionText"
                   HorizontalAlignment="Center"
                   Margin="0,4,0,16"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
        <TextBlock x:Name="BodyText"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>
        <TextBlock x:Name="ContactBlock"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>
        <TextBlock x:Name="CreditBlock" TextWrapping="Wrap"/>
    </StackPanel>
</Window>
